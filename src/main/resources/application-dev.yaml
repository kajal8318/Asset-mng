server:
  port: 8081

spring:
  application:
    name: asset-management-system

  datasource:
    url: jdbc:postgresql://staging-db.cluster-ct8ao6q4s7fd.ap-south-1.rds.amazonaws.com:5432/new_joiners
    username: kajal
    password: Kajal123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true